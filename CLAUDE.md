# FRAME VK Bot

> ⛔️ **ГРАНИЦА ПРОЕКТА — ПРОЧИТАЙ ПЕРВЫМ**
> Это **ТОЛЬКО VK-бот**. Репозиторий: `github.com/khlopenik/vk-bot`.
> Telegram-версия — **отдельный проект** в папке `~/Desktop/ПРОГРАММИРОВАНИЕ/Бот FRAME/` (репо `tg-bot`).
> - НЕ пушь VK-код в репозиторий `tg-bot` и не редактируй файлы в папке `Бот FRAME`.
> - Если задача про Telegram — ты открыл не ту папку. Остановись и переключись.
> - БД Supabase общая с TG, но код ботов раздельный. Не смешивай.

## Стек
- Python 3, vk-api (Long Poll), Flask (YooKassa webhook)
- Supabase (общая БД с TG-ботом) — VK user_id хранится как `vk_id + 10_000_000_000`
- MuAPI — та же что и в TG-боте
- YooKassa — платёж через redirect link (не инлайн как в TG)
- Деплой: Render.com

## Файлы
- `vk_bot.py` — всё в одном файле (бот + Flask webhook)

## Env переменные (Render)
- `VK_TOKEN` — токен сообщества ВКонтакте
- `MUAPI_KEY` — тот же что в TG-боте
- `SUPABASE_URL` — тот же что в TG-боте
- `SUPABASE_KEY` — тот же что в TG-боте
- `YOKASSA_SHOP_ID` — тот же что в TG-боте
- `YOKASSA_KEY` — тот же что в TG-боте
- `ADMIN_VK_ID` — ВКонтакте ID администратора
- `PORT` — порт Flask (Render задаёт автоматически)

## Supabase
- Таблица `user_credits` — та же что в TG, VK IDs смещены на +10_000_000_000
- Таблица `generation_history` — та же

## Как получить VK_TOKEN
1. Создать сообщество ВКонтакте
2. Управление → Работа с API → Создать ключ
3. Права: Сообщения сообщества + Фотографии
4. Включить «Сообщения» в настройках сообщества

## YooKassa для VK
- Бот создаёт платёжную ссылку (confirmation_url) и отправляет пользователю
- После оплаты YooKassa делает POST на `/yookassa-webhook-vk`
- Нужно настроить webhook URL в ЮKassa: `https://<render-url>/yookassa-webhook-vk`

## Профиль и уведомления
- `register_vk_user(vk, vk_id)` вызывается в главном цикле один раз за процесс на пользователя:
  тянет имя через `vk.users.get` и пишет в `user_credits` (`tg_name`/`tg_username`) — чтобы
  пользователь был виден по имени в админке TG-бота (БД общая).
- Если пользователь новый (нет строки в `user_credits`) — шлёт уведомление админу на `ADMIN_VK_ID`.
- Существующие безымянные VK-юзеры получают имя при следующем сообщении (бэкофилл через тот же upsert).

## Поток пользователя
1. Пользователь пишет боту → приветствие + главное меню
2. «📸 Генерация фото» → отправь фото
3. Фото получено → выбор модели (⭐/✨/💎/🔷)
4. Выбор модели → ввод промпта (или «-»)
5. Промпт → генерация → результат отправляется фото-вложением
